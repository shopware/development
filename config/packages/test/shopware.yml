shopware:
    filesystem:
        private:
            type: "memory"
            config:
                root: ~
        public:
            type: "memory"
            config:
                root: ~